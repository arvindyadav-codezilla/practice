services:
  - type: web
    name: synapse-chat-backend
    env: python
    plan: free
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: SUPABASE_URL
        value: https://swxgavhlldmwmfctaiag.supabase.co
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: TEXTMEBOT_API_KEY
        value: YANdof7NonNU
